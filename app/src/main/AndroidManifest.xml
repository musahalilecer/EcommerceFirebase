<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcommerceFirebase"
        tools:targetApi="31">
        <activity
            android:name=".activity.ShoppingCardActivity"
            android:exported="false"
            android:label="@string/title_activity_shopping_card"
            android:theme="@style/Theme.EcommerceFirebase" />
        <activity
            android:name=".activity.AccountActivity"
            android:exported="false"
            android:label="@string/title_activity_account"
            android:theme="@style/Theme.EcommerceFirebase" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.EcommerceFirebase">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ProductAddActivity"
            android:exported="true"
            android:label="EcommerceFirebase"
            android:theme="@style/Theme.EcommerceFirebase" />
    </application>

</manifest>